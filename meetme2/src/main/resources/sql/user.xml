<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Users">
	<select resultType="User" parameterType="String" id="idcheck">
		select * from mm_user where user_id = #{user_id}
	</select>
	
	<insert parameterType="User" id="insert">
		insert into mm_user 
		values((select nvl(max(user_num),0)+1 from mm_user), #{user_id},#{user_pass},#{user_name},#{user_email},#{user_gender},#{user_age},#{user_city}, #{user_image})
	</insert>
	
	
	
</mapper>


