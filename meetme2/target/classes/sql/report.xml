<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Report">
	<select id="reportList" resultType="report">
		select * from report;
	</select>
	
	<select id="userCount" resultType="int">
		select count(*)
		from mm_report
		where user_num is not null and event_num is null
	</select>
	
	<select id="eventCount" resultType="int">
		select count(*)
		from mm_report
		where user_num is null and event_num is not null
	</select>
	
	<select id="userReport" resultType="report">
		select report_num, reporter_id, mm_user.user_id, mm_report.user_num, report_content, report_date 
		from mm_report 
		inner join mm_user 
		on mm_report.user_num=mm_user.user_num
		order by report_date desc
	</select>
	
	<select id="eventReport" resultType="report">
		select report_num, reporter_id, report_content, mm_report.event_num, mm_event.event_title, report_date, mm_event.user_num
		from mm_report 
		inner join MM_EVENT
		on mm_report.event_num=mm_event.event_num
		order by report_date desc
	</select>
</mapper>


