<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="User_interests">
	<select id="dashInter" parameterType="int" resultType="Dash">
		select (mm_user_interests.category_num)count, 
				mm_category.category_name 
		from mm_user_interests 
		inner join mm_category
		on mm_user_interests.category_num=#{category_num}
			and mm_category.category_num=#{category_num}
	</select>
</mapper>


